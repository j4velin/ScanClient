package de.j4velin.scanclient.ui.theme

import androidx.compose.ui.graphics.Color

// The View theme's three colours, kept so the app still looks like itself: colorPrimary #455A64
// (blue grey 700) and colorAccent #FF5722 (deep orange 500). The rest of each scheme is what
// Material 3 derives around them.
val BlueGrey700 = Color(0xFF455A64)
val BlueGrey900 = Color(0xFF30454F)
val BlueGrey100 = Color(0xFFCFD8DC)
val BlueGrey200 = Color(0xFFB0BEC5)

// Theme.Material's own neutrals. Material 3 derives its surfaces from the primary instead, which
// tinted the light window faintly pink and made the dark one near-black; the platform theme used
// #FAFAFA and #303030, with #424242 for anything floating above them.
val Grey50 = Color(0xFFFAFAFA)
val Grey200 = Color(0xFFEEEEEE)
val Grey400 = Color(0xFFBDBDBD)
val Grey600 = Color(0xFF757575)
val Grey800 = Color(0xFF424242)
val Grey850 = Color(0xFF303030)
val Grey900 = Color(0xFF212121)

val DeepOrange500 = Color(0xFFFF5722)
val DeepOrange200 = Color(0xFFFFAB91)
val DeepOrange900 = Color(0xFFBF360C)
